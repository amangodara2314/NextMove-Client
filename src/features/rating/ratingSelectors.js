export const selectRatings = (state) => state.rating.ratings;

export const selectLoadingRatings = (state) => state.rating.loadingRatings;

export const selectRatingsError = (state) => state.rating.ratingsError;

export const selectRating = (state) => state.rating;
